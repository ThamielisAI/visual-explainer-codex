---
description: Generate a seamless, tileable texture or pattern.
---

Use the MCP `generate_pattern` tool to create a seamless pattern for `$ARGUMENTS`, following the detailed guidance in `commands/visual-pattern.toml`. Report the saved output path.
