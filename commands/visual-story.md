---
description: Generate a visually consistent sequence of images for a narrative.
---

Use the MCP `generate_image` tool to create the requested visual story for `$ARGUMENTS`, following the detailed guidance in `commands/visual-story.toml` and preserving style consistency across frames.
