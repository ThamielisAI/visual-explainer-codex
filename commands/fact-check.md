---
description: Verify and correct a document's claims against the current codebase.
---

Use the `visual-explainer-extension` skill to fact-check `$ARGUMENTS` against the repository and correct the document in place when requested by the command workflow.

Follow the detailed workflow in `commands/fact-check.toml`, including the verification summary and evidence links.
