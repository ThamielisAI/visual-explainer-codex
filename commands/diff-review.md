---
description: Turn a git diff, commit, branch, or pull request into a visual code review.
---

Use the `visual-explainer-extension` skill to create a self-contained HTML diff review for `$ARGUMENTS`.

Follow the detailed workflow in `commands/diff-review.toml`, verify every factual claim against the repository, and write the result under `~/.agent/diagrams/`.
