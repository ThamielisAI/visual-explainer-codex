---
description: Generate a technical archetype card and component breakdown for a module or subsystem.
---

Use the `visual-explainer-extension` skill to create a self-contained HTML archetype breakdown for `$ARGUMENTS`.

Follow the detailed workflow in `commands/code-archetype.toml`, including the generated module icon, component cards, integration points, and output under `~/.agent/diagrams/`.
