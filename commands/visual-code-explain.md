---
description: Explain a code file by mapping natural-language intent to implementation.
---

Use the `visual-explainer-extension` skill to explain `$ARGUMENTS` as a self-contained HTML page.

Follow the detailed workflow in `commands/visual-code-explain.toml`, including intent bridges, structural analysis, and a generated archetype icon when the MCP image tool is available.
