---
description: Generate a magazine-quality self-contained HTML slide deck.
---

Use the `visual-explainer-extension` skill to generate a slide deck for `$ARGUMENTS` as a self-contained HTML file.

Follow the detailed workflow in `commands/generate-slides.toml`, use the slide-deck template and reference materials, and write the result under `~/.agent/diagrams/`.
