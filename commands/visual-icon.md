---
description: Generate an app icon or UI element in a chosen visual style.
---

Use the MCP `generate_icon` tool to create an icon for `$ARGUMENTS`, following the detailed guidance in `commands/visual-icon.toml`. Report the saved output path.
