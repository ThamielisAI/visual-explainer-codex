---
description: Review an implementation plan against the actual codebase before work begins.
---

Use the `visual-explainer-extension` skill to review `$ARGUMENTS` against the current repository.

Follow the detailed workflow in `commands/plan-review.toml`, verify current-state claims, identify missing dependencies and risks, and write the result under `~/.agent/diagrams/`.
