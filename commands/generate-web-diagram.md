---
description: Generate a polished self-contained HTML diagram for a topic.
---

Use the `visual-explainer-extension` skill to generate a professional web diagram for `$ARGUMENTS`.

Follow the detailed workflow in `commands/generate-web-diagram.toml`, choose an intentional visual system, use the relevant template and references, and write the result under `~/.agent/diagrams/`.
