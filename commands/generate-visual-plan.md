---
description: Turn an implementation plan into a visual current-versus-planned review.
---

Use the `visual-explainer-extension` skill to generate a visual plan review for `$ARGUMENTS`.

Follow the detailed workflow in `commands/generate-visual-plan.toml`, including current and planned architecture, dependency ripple analysis, and risk review.
