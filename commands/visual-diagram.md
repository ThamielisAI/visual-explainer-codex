---
description: Generate a professional diagram or flowchart as an image.
---

Use the MCP `generate_image` tool to create a professional diagram for `$ARGUMENTS`, following the detailed guidance in `commands/visual-diagram.toml`.
