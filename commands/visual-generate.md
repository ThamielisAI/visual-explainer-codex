---
description: Generate a high-quality image from a descriptive prompt.
---

Use the MCP `generate_image` tool to generate the image requested in `$ARGUMENTS`, following the detailed guidance in `commands/visual-generate.toml`. Report the saved output paths.
