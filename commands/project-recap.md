---
description: Create a visual project recap for returning to a codebase after a context switch.
---

Use the `visual-explainer-extension` skill to create a project recap for `$ARGUMENTS`.

Follow the detailed workflow in `commands/project-recap.toml`, verify all quantitative and architectural claims, and write the result under `~/.agent/diagrams/`.
